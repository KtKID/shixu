# task-e2e-sync-flows · 开发清单

> spec: docs/spec/e2e/spec.md
> 创建: 2026-09-12

**状态**：`[ ] ⏳` 未开始 / `[ ] ▶️` 进行中 / `[x] 🟢` 验证通过 / `[!] 🔴` 验证失败

覆盖 feat04（导入链路验证，2 场景）、feat05（登录与同步链路验证，4 场景）、feat07（多库与登出验证，2 场景）。依赖 task-e2e-harness 的服务器/浏览器夹具与探针闸门；task-e2e-snapshot-capture 的登录态搭设复用本 task 的链路辅助。

| #   | 任务                                                                                                                                                 | 场景回指            | 涉及文件                     | 风险 | 状态   |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ---------------------------- | ---- | ------ |
| T1  | 浏览器书签预置夹具：向一次性浏览器档案预置 3 条书签（浏览器启动前写入档案书签数据，随夹具生命周期清理）                                              | feat04 场景1        | e2e/fixtures/profile-seed.ts | None | [ ] ⏳ |
| T2  | 导入链路用例：主页导入区勾选 3 条预置书签导入 → 收藏页出现、条数 3、状态 Inbox；重复导入同一条仍只有一份                                             | feat04 场景1, 场景2 | e2e/tests/import.spec.ts     | None | [ ] ⏳ |
| T3  | 服务器夹具补暂停/恢复：临时停机与恢复能力，供同步失败场景切换服务器可达性（恢复后不留残留）                                                          | feat05 场景4        | e2e/fixtures/server.ts       | None | [ ] ⏳ |
| T4  | 注册登录用例：「网络连接」注册新账号并登录 → 界面切到空账号库、默认库 3 条不显示                                                                     | feat05 场景1        | e2e/tests/sync-flow.spec.ts  | None | [ ] ⏳ |
| T5  | 同步呈现用例：登录后导入 1 条并点「同步收藏」→ 云朵已同步 + 页头「已全部同步」+ 账号卡服务器条数；再导入 1 条不同步 → 页头待同步计数、「待同步」筛出 | feat05 场景2, 场景3 | e2e/tests/sync-flow.spec.ts  | None | [ ] ⏳ |
| T6  | 同步失败不误报用例：停服务器 → 同步失败云朵保持待同步 → 恢复服务器再同步 → 云朵翻回已同步                                                            | feat05 场景4        | e2e/tests/sync-flow.spec.ts  | None | [ ] ⏳ |
| T7  | 多库与登出用例：登出 → 回默认库 3 条原样；未登录界面无任何同步元素（无云朵、页头只有条数、无「待同步」入口、无快照区、存档按钮不可用）               | feat07 场景1, 场景2 | e2e/tests/multi-lib.spec.ts  | None | [ ] ⏳ |

## 影响文件树

```text
x-threadpick/
└── e2e/
    ├── fixtures/
    │   ├── profile-seed.ts          U  # 新增：向一次性档案预置 3 条浏览器书签
    │   └── server.ts                M  # 增加临时停机与恢复能力（同步失败场景）
    └── tests/
        ├── import.spec.ts           U  # 新增：导入与重复导入用例
        ├── sync-flow.spec.ts        U  # 新增：注册登录、同步呈现、待同步筛选、失败不误报用例
        └── multi-lib.spec.ts        U  # 新增：登出回默认库、未登录无同步元素用例
```
