# task-e2e-harness · 开发清单

> spec: docs/spec/e2e/spec.md
> 创建: 2026-09-12

**状态**：`[ ] ⏳` 未开始 / `[ ] ▶️` 进行中 / `[x] 🟢` 验证通过 / `[!] 🔴` 验证失败

覆盖 feat01（一条命令跑完端到端验证，4 场景）、feat02（全新的隔离运行环境，4 场景）、feat03（真实浏览器装载扩展，2 场景）、feat09（失败现场可复查，2 场景）。本 task 是其余两个 task 的地基：e2e 顶层包接线 + 环境自检 + 服务器/浏览器夹具 + 运行编排 + 探针用例。

| #   | 任务                                                                                                                                                                                               | 场景回指                          | 涉及文件                                                                           | 风险 | 状态   |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ---------------------------------------------------------------------------------- | ---- | ------ |
| T1  | 环境自检与引导：验证入口启动前先检查测试工具与测试浏览器是否已存在——已存在直接复用、不重新下载不重复安装；缺失时输出一次性安装指引并终止本轮，不静默安装                                           | feat01 场景3, 场景4               | e2e/scripts/ensure-env.ts                                                          | None | [ ] ⏳ |
| T2  | 工作区接线：新建 e2e 顶层包接入 workspace（不提供 test script，不混入 `pnpm test` 门禁）；tsconfig strict 同仓库基线；根命令 `pnpm e2e` 一条入口；e2e 产物目录进 .gitignore                        | feat01 场景1                      | pnpm-workspace.yaml, package.json, .gitignore, e2e/package.json, e2e/tsconfig.json | None | [ ] ⏳ |
| T3  | 服务器夹具：每轮独立临时数据目录 → 按当前结构建表 → 启动一次性服务器（自动选空闲端口）→ 健康检查等待就绪 → 结束（含失败）终止整组进程并按清理策略处理数据目录                                      | feat02 场景2, 场景3, 场景4        | e2e/fixtures/server.ts                                                             | None | [ ] ⏳ |
| T4  | 浏览器夹具：一次性临时浏览器档案装载扩展（仅装本扩展），等待扩展后台就绪并解析扩展标识；提供打开扩展主页（带分区定位）与弹窗的辅助；档案随轮按清理策略处理                                         | feat02 场景1, 场景3, feat03 场景1 | e2e/fixtures/browser.ts                                                            | None | [ ] ⏳ |
| T5  | 运行编排与结果汇总：`pnpm e2e` 一条命令串「环境自检 → 扩展准备成最新可装载版本 → 起服务器 → 跑浏览器用例」；单场景失败记录后继续执行其余场景，末尾汇总通过数、失败清单与总耗时                     | feat01 场景1, 场景2               | e2e/playwright.config.ts, e2e/package.json                                         | None | [ ] ⏳ |
| T6  | 装载探针用例（全量用例的前置闸门）：扩展后台就绪、主页可打开、弹窗可打开；装载失败时明确报错并使本轮判败，不产出「通过」结果                                                                       | feat03 场景1, 场景2               | e2e/tests/probe.spec.ts                                                            | None | [ ] ⏳ |
| T7  | 现场保留与清理策略：场景失败自动留存该步页面截图与现场数据目录路径（浏览器档案与服务器数据可按路径人工打开排查）；全绿轮次清理一切临时产物，只留结果摘要；不影响日常开发数据与正在运行的日常服务器 | feat09 场景1, 场景2, feat02 场景3 | e2e/playwright.config.ts, e2e/fixtures/server.ts, e2e/fixtures/browser.ts          | None | [ ] ⏳ |
| T8  | 文档：AGENTS.md 补 e2e 一条命令运行说明与产物位置                                                                                                                                                  | None                              | AGENTS.md                                                                          | None | [ ] ⏳ |

## 影响文件树

```text
x-threadpick/
├── .gitignore                     M  # 忽略 e2e 产物（测试结果、报告、临时目录）
├── AGENTS.md                      M  # 补 e2e 运行说明
├── package.json                   M  # 根命令 e2e（内含扩展构建前置）
├── pnpm-workspace.yaml            M  # workspace 接入 e2e 包
└── e2e/
    ├── package.json               U  # 新增：e2e 包（工具依赖、e2e script；无 test script 不进 pnpm test）
    ├── tsconfig.json              U  # 新增：strict 同仓库基线
    ├── playwright.config.ts       U  # 新增：串行编排、失败继续、结果汇总、现场保留策略
    ├── scripts/
    │   └── ensure-env.ts          U  # 新增：测试工具与浏览器存在性检查（已存在复用，缺失给一次性安装指引）
    ├── fixtures/
    │   ├── server.ts              U  # 新增：临时数据建表、一次性服务器起停、空闲端口、清理
    │   └── browser.ts             U  # 新增：临时档案装载扩展、后台就绪等待、主页/弹窗辅助
    └── tests/
        └── probe.spec.ts          U  # 新增：装载探针（后台就绪 + 主页可开 + 弹窗可开）
```
